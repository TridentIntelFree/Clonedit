{
  "name": "jbh-88",
  "version": "0.96.0",
  "private": true,
  "description": "Offline sampler, sequencer and groovebox",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm test && vite build && node scripts/emit.mjs && node scripts/smoke.mjs",
    "preview": "vite preview",
    "test": "node --test 'tests/*.test.mjs'"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "esbuild": "^0.28.1",
    "playwright-core": "^1.62.0",
    "svelte": "^5.56.8",
    "vite": "^8.1.5",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
